services:
  app:
    build: .
    ports:
      - "9000:9000"
    environment:
      - NODE_ENV=production
      - PORT=9000
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - .env:/app/.env
    restart: unless-stopped
