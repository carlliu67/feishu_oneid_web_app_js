services:
  app:
    build: .
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - NODE_ENV=production
      - PORT=9001
    volumes:
      - ./data:/app/data
      - .env:/app/.env
    restart: unless-stopped
